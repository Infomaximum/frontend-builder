import CopyWebpackPlugin from "copy-webpack-plugin";

module.exports = (PATHS: any) => {
  return {
    // plugins: [new CopyWebpackPlugin()] // копирование каких либо ресурсов (шрифты, картинки)
  };
};
